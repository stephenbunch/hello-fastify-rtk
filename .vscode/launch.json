{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Node: debug server-side",
      "type": "node-terminal",
      "request": "launch",
      "command": "yarn dev",
      "cwd": "${workspaceFolder}/server"
    }
  ]
}
